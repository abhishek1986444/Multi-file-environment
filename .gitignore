# Ignore all files by default
*

# Allow .cpp, .hpp, and .bat files
!*.cpp
!*.hpp
!*.bat

# Allow the .gitignore file itself
!.gitignore

#
#git add .gitignore
# git commit -m "Add .gitignore to track only .cpp, .hpp, and .bat files"

# git status --ignored 